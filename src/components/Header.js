import React from 'react';
import AddPostForm from './AddPostForm';

const Header = () => (
    <header className="header">
        <h1 className="page-title">Example Blog</h1>
        <AddPostForm />
    </header>
);

export default Header;
